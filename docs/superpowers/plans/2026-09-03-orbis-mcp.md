# Orbis MCP Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Expor o CRM do portal como 16 tools MCP em `POST /mcp`, com auth por token Bearer.

**Architecture:** SDK `@modelcontextprotocol/sdk` em modo stateless (`WebStandardStreamableHTTPServerTransport` com `sessionIdGenerator: undefined`, um `McpServer` novo por request) dentro do Worker existente. Tools reutilizam os schemas zod e o SQL das rotas; cada `tools/call` grava `audit()`.

**Tech Stack:** `@modelcontextprotocol/sdk@^1.30` (verificado no tarball: exports `server/mcp.js` e `server/webStandardStreamableHttp.js`), `zod@^3.25` (peer exigido pelo SDK: `^3.25 || ^4.0`; o repo hoje tem `^3.23.8`, bump minor sem breaking), Hono, D1.

**Spec:** `docs/superpowers/specs/2026-09-03-orbis-mcp-design.md`

## Global Constraints

- Nomes de tools com no máximo 64 caracteres (limite da API; nunca estourar).
- Mensagens de erro visíveis ao usuário em PT-BR, nunca stack trace.
- TDD: teste falha primeiro, implementação mínima, suíte inteira verde.
- `POST /mcp` aceita só Bearer; cookie nunca autentica `/mcp`.
- Sem `delete_processo` (arquivar via `update_processo`).

---

## File Structure

- Create `migrations/0006_api_tokens.sql` — tabela `api_tokens`.
- Create `src/routes/tokens.ts` — `POST/GET/DELETE /api/tokens` (sessão web, usa `actor` do `requireAuth`).
- Create `src/mcp/auth.ts` — `verifyBearer(db, header)` → `user_id | null`.
- Create `src/mcp/server.ts` — `ToolCtx`, helper `tool()`, `buildMcpServer(ctx)`.
- Create `src/mcp/tools/processos.ts`, `clientes.ts`, `prazos.ts`, `tarefas.ts`, `dashboard.ts` — um `registerX(server, ctx)` por arquivo.
- Modify `src/routes/clientes.ts`, `processos.ts`, `tarefas.ts`, `prazos.ts` — adicionar `export` aos schemas/consts existentes (1 palavra por declaração, sem renomear).
- Modify `src/index.ts` — montar `/api/tokens`, handler `app.all('/mcp', …)`.
- Modify `package.json` — via npm (Task 2).
- Test `tests/tokens.test.ts`, `tests/mcp.test.ts`, `tests/mcp_tools.test.ts`.

---

### Task 1: Tokens (migration + gerência)

**Files:**
- Create: `migrations/0006_api_tokens.sql`
- Create: `src/routes/tokens.ts`
- Modify: `src/index.ts` (1 linha: montar rota)
- Test: `tests/tokens.test.ts`

**Interfaces:**
- Consumes: `requireAuth` (já monta `actor`), `audit()`, `err()`.
- Produces: tabela `api_tokens`; `POST /api/tokens` → `{ token }` em claro uma única vez.

- [ ] **Step 1: Write migration**

```sql
CREATE TABLE IF NOT EXISTS api_tokens (
  id TEXT PRIMARY KEY,
  user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  nome TEXT NOT NULL DEFAULT '',
  token_hash TEXT NOT NULL UNIQUE,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  revogado_em TEXT
);
```

- [ ] **Step 2: Write the failing test** (`tests/tokens.test.ts`)

```typescript
import { describe, it, expect } from 'vitest';
import app from '../src/index';

function makeDb() {
  const seen: string[] = [];
  const db = {
    prepare: (sql: string) => {
      seen.push(sql);
      return {
        bind: (..._args: unknown[]) => ({
          first: async () => {
            if (/FROM sessions/.test(sql)) return { user_id: 'u1', expires_at: '2999-01-01T00:00:00.000Z' };
            return null;
          },
          all: async () => ({ results: [] }),
          run: async () => ({ success: true }),
        }),
      };
    },
  };
  return { db: db as never, seen };
}
const authed = { 'content-type': 'application/json', cookie: 'orbis_session=fake' };

describe('POST /api/tokens', () => {
  it('creates token and shows value once', async () => {
    const { db, seen } = makeDb();
    const res = await app.request('/api/tokens', {
      method: 'POST', headers: authed, body: JSON.stringify({ nome: 'claude-code' }),
    }, { DB: db });
    expect(res.status).toBe(201);
    expect(seen.some((s) => /INSERT INTO api_tokens/.test(s))).toBe(true);
    const body = await res.json() as { token: string };
    expect(body.token.startsWith('orbis_')).toBe(true);
  });
  it('rejects without session', async () => {
    const { db } = makeDb();
    const res = await app.request('/api/tokens', {
      method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({}),
    }, { DB: db });
    expect(res.status).toBe(401);
  });
});

describe('GET /api/tokens', () => {
  it('lists without exposing hashes', async () => {
    const { db } = makeDb();
    const res = await app.request('/api/tokens', { headers: authed }, { DB: db });
    expect(res.status).toBe(200);
    const text = await res.text();
    expect(/token_hash/.test(text)).toBe(false);
  });
});

describe('DELETE /api/tokens/:id', () => {
  it('revokes instead of deleting', async () => {
    const { db, seen } = makeDb();
    const res = await app.request('/api/tokens/tok_1', { method: 'DELETE', headers: authed }, { DB: db });
    expect(res.status).toBe(200);
    expect(seen.some((s) => /UPDATE api_tokens SET revogado_em/.test(s))).toBe(true);
  });
});
```

- [ ] **Step 3: Run test to verify it fails**

Run: `npx vitest run tests/tokens.test.ts`
Expected: FAIL — 404 em todas (rota ainda não existe).

- [ ] **Step 4: Write minimal implementation** (`src/routes/tokens.ts`)

```typescript
import { Hono } from 'hono';
import { z } from 'zod';
import { audit } from '../lib/auth';
import { err } from '../lib/errors';
import type { Env } from '../index';

export const tokens = new Hono<{ Bindings: Env }>();

export async function hashToken(token: string): Promise<string> {
  const digest = await crypto.subtle.digest('SHA-256', new TextEncoder().encode(token));
  return [...new Uint8Array(digest)].map((b) => b.toString(16).padStart(2, '0')).join('');
}

function actorId(c: { get: (k: never) => unknown }): string {
  return c.get('actor' as never) as string;
}

tokens.post('/', async (c) => {
  try {
    const body = z.object({ nome: z.string().max(60).default('') }).parse(await c.req.json());
    const uid = actorId(c);
    const raw = [...crypto.getRandomValues(new Uint8Array(24))].map((b) => b.toString(16).padStart(2, '0')).join('');
    const token = `orbis_${raw}`;
    const id = `tok_${Math.random().toString(36).slice(2, 10)}`;
    await c.env.DB.prepare(`INSERT INTO api_tokens (id, user_id, nome, token_hash) VALUES (?, ?, ?, ?)`)
      .bind(id, uid, body.nome, await hashToken(token)).run();
    await audit(c.env.DB, uid, 'token_create', 'api_token', id);
    return c.json({ data: { id, nome: body.nome }, token }, 201);
  } catch (e) {
    if (e instanceof z.ZodError) return err(e, 'invalid_token', 400);
    return err(e);
  }
});

tokens.get('/', async (c) => {
  const rows = await c.env.DB.prepare(
    `SELECT id, nome, substr(token_hash, 1, 10) AS prefixo, created_at, revogado_em FROM api_tokens WHERE user_id = ? ORDER BY created_at DESC LIMIT 50`
  ).bind(actorId(c)).all();
  return c.json({ data: rows.results });
});

tokens.delete('/:id', async (c) => {
  await c.env.DB.prepare(`UPDATE api_tokens SET revogado_em = datetime('now') WHERE id = ? AND user_id = ?`)
    .bind(c.req.param('id'), actorId(c)).run();
  await audit(c.env.DB, actorId(c), 'token_revoke', 'api_token', c.req.param('id'));
  return c.json({ ok: true });
});
```

Mount in `src/index.ts` (1 linha, junto às outras rotas):

```typescript
import { tokens } from './routes/tokens';
app.route('/api/tokens', tokens as never);
```

- [ ] **Step 5: Run test to verify it passes**

Run: `npx vitest run tests/tokens.test.ts` — Expected: PASS (4 testes).
Run: `npm test` — Expected: suíte inteira verde.

- [ ] **Step 6: Commit**

```bash
git add migrations/0006_api_tokens.sql src/routes/tokens.ts src/index.ts tests/tokens.test.ts
git commit -m "feat: api tokens com gerencia por sessao

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 2: Transporte `/mcp` + auth Bearer

**Files:**
- Create: `src/mcp/auth.ts`
- Create: `src/mcp/server.ts`
- Modify: `src/index.ts` (handler `/mcp`), `package.json` (via npm)
- Test: `tests/mcp.test.ts`

**Interfaces:**
- Consumes: `api_tokens` (Task 1).
- Produces: `verifyBearer(db, header)`; `buildMcpServer(ctx)`; `POST /mcp` operacional (lista vazia ainda).

- [ ] **Step 1: Install SDK**

Run: `npm install @modelcontextprotocol/sdk zod@^3.25`
Verify: `npm ls zod` mostra uma única v3.25+; `npm ls @modelcontextprotocol/sdk` ok.

- [ ] **Step 2: Write the failing test** (`tests/mcp.test.ts`)

```typescript
import { describe, it, expect } from 'vitest';
import app from '../src/index';

function makeDb(opts: { valid?: boolean } = {}) {
  const db = {
    prepare: (sql: string) => ({
      bind: (..._args: unknown[]) => ({
        first: async () => {
          if (/FROM api_tokens/.test(sql)) return opts.valid === false ? null : { user_id: 'u1' };
          if (/FROM sessions/.test(sql)) return { user_id: 'u1', expires_at: '2999-01-01T00:00:00.000Z' };
          return null;
        },
        all: async () => ({ results: [] }),
        run: async () => ({ success: true }),
      }),
    }),
  };
  return db as never;
}

async function rpc(db: never, body: unknown, token = 'Bearer orbis_test') {
  const res = await app.request('/mcp', {
    method: 'POST',
    headers: { 'content-type': 'application/json', accept: 'application/json, text/event-stream', authorization: token },
    body: JSON.stringify(body),
  }, { DB: db });
  const text = await res.text();
  const line = text.split('\n').find((l) => l.startsWith('data: ')) ?? '';
  return { status: res.status, json: JSON.parse(line.slice(6)) as { result?: { tools: Array<{ name: string }> }, error?: unknown } };
}

describe('POST /mcp', () => {
  it('lists registered tools with valid token', async () => {
    const { json } = await rpc(makeDb(), { jsonrpc: '2.0', id: 1, method: 'tools/list' });
    expect(json.result?.tools.length).toBe(0);
  });
  it('rejects without token', async () => {
    const res = await app.request('/mcp', {
      method: 'POST',
      headers: { 'content-type': 'application/json' },
      body: JSON.stringify({ jsonrpc: '2.0', id: 1, method: 'tools/list' }),
    }, { DB: makeDb() });
    expect(res.status).toBe(401);
  });
  it('rejects revoked token', async () => {
    const { status } = await rpc(makeDb({ valid: false }), { jsonrpc: '2.0', id: 1, method: 'tools/list' });
    expect(status).toBe(401);
  });
  it('rejects cookie session on /mcp', async () => {
    const res = await app.request('/mcp', {
      method: 'POST',
      headers: { 'content-type': 'application/json', cookie: 'orbis_session=fake' },
      body: JSON.stringify({ jsonrpc: '2.0', id: 1, method: 'tools/list' }),
    }, { DB: makeDb() });
    expect(res.status).toBe(401);
  });
  it('returns 405 for GET', async () => {
    const res = await app.request('/mcp', { headers: { authorization: 'Bearer orbis_test' } }, { DB: makeDb() });
    expect(res.status).toBe(405);
  });
});
```

- [ ] **Step 3: Run test to verify it fails**

Run: `npx vitest run tests/mcp.test.ts`
Expected: FAIL — 404 (sem handler `/mcp`).

- [ ] **Step 4: Write minimal implementation**

`src/mcp/auth.ts`:

```typescript
export async function verifyBearer(db: D1Database, header: string | undefined): Promise<string | null> {
  const m = (header ?? '').match(/^Bearer ([A-Za-z0-9_.-]+)$/);
  if (!m) return null;
  const digest = [...new Uint8Array(await crypto.subtle.digest('SHA-256', new TextEncoder().encode(m[1])))]
    .map((b) => b.toString(16).padStart(2, '0')).join('');
  const row = await db.prepare(`SELECT user_id FROM api_tokens WHERE token_hash = ? AND revogado_em IS NULL`)
    .bind(digest).first<{ user_id: string }>();
  return row?.user_id ?? null;
}
```

`src/mcp/server.ts` (vazio ainda — tools vêm nas Tasks 3–4):

```typescript
import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';

export type ToolCtx = { db: D1Database; userId: string };

export function buildMcpServer(_ctx: ToolCtx): McpServer {
  return new McpServer({ name: 'orbis-mcp', version: '1.0.0' });
}
```

`src/index.ts` (após as rotas `/api/*`, antes de `/api/login`):

```typescript
import { WebStandardStreamableHTTPServerTransport } from '@modelcontextprotocol/sdk/server/webStandardStreamableHttp.js';
import { buildMcpServer } from './mcp/server';
import { verifyBearer } from './mcp/auth';

app.all('/mcp', async (c) => {
  if (c.req.method !== 'POST') {
    return Response.json({ jsonrpc: '2.0', error: { code: -32000, message: 'Method not allowed.' }, id: null }, { status: 405 });
  }
  const userId = await verifyBearer(c.env.DB, c.req.header('authorization'));
  if (!userId) return c.json({ error: 'unauthorized', code: 'unauthorized', requestId: 'mcp' }, 401);
  const server = buildMcpServer({ db: c.env.DB, userId });
  const transport = new WebStandardStreamableHTTPServerTransport({ sessionIdGenerator: undefined });
  await server.connect(transport);
  return transport.handleRequest(c.req.raw);
});
```

- [ ] **Step 5: Run test to verify it passes**

Run: `npx vitest run tests/mcp.test.ts` — Expected: PASS (5 testes).
Run: `npm test` — Expected: suíte inteira verde.

- [ ] **Step 6: Commit**

```bash
git add package.json package-lock.json src/mcp/ src/index.ts tests/mcp.test.ts
git commit -m "feat: transporte MCP stateless em POST /mcp

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 3: Tools de leitura (7)

**Files:**
- Create: `src/mcp/tools/processos.ts`, `clientes.ts`, `prazos.ts`, `tarefas.ts`, `dashboard.ts`
- Modify: `src/mcp/server.ts` (chamar os 5 `register*`), `src/routes/*.ts` (só adicionar `export` aos schemas)
- Test: `tests/mcp_tools.test.ts` (parte 1)

**Interfaces:**
- Consumes: `ToolCtx`, schemas exportados das rotas, SQL já existente nas rotas (copiar o SELECT, não reinventar).
- Produces: 7 tools registradas; helper `tool()` em `server.ts`.

Helper `tool()` (adicionar em `src/mcp/server.ts`):

```typescript
import { audit } from '../lib/auth';

export function tool(
  server: McpServer,
  ctx: ToolCtx,
  name: string,
  description: string,
  shape: Record<string, z.ZodTypeAny>,
  run: (args: Record<string, unknown>) => Promise<unknown>,
): void {
  server.registerTool(name, { description, inputSchema: shape }, async (args) => {
    try {
      const data = await run(args as Record<string, unknown>);
      await audit(ctx.db, ctx.userId, `mcp.${name}`, 'mcp', name, {});
      return { content: [{ type: 'text' as const, text: JSON.stringify(data) }] };
    } catch (e) {
      return { content: [{ type: 'text' as const, text: e instanceof Error ? e.message : String(e) }], isError: true };
    }
  });
}
```

(`import type { z } from 'zod'` no topo para o tipo.)

- [ ] **Step 1: Exportar schemas das rotas** (4 edições de 1 palavra)

Em `src/routes/clientes.ts`: `const schema` → `export const schema`. Em `processos.ts`, `tarefas.ts`, `prazos.ts`: idem. (Nomes colidem entre módulos? Não — cada import usa alias: `import { schema as clienteSchema } from '../routes/clientes';`.)

- [ ] **Step 2: Write the failing tests** (anexar em `tests/mcp_tools.test.ts`; reaproveitar o helper `rpc()` da Task 2 copiando as ~10 linhas para este arquivo)

```typescript
it('search_processos returns matching rows', async () => {
  // fakeDb: api_tokens → { user_id: 'u1' }; SELECT processos → all() [{ id: 'pro_1', numero_cnj: '...' }]
  const { json } = await rpc(dbComProcessos, { jsonrpc: '2.0', id: 2, method: 'tools/call',
    params: { name: 'search_processos', arguments: { q: '0000' } } });
  const text = json.result.content[0].text as string;
  expect(text).toContain('pro_1');
});
it('get_cliente unknown id returns readable error', async () => {
  // fakeDb: SELECT clientes → first() null
  const { json } = await rpc(dbVazio, { jsonrpc: '2.0', id: 3, method: 'tools/call',
    params: { name: 'get_cliente', arguments: { id: 'missing_1' } } });
  expect(json.result.isError).toBe(true);
  const text = json.result.content[0].text as string;
  expect(text).toMatch(/não encontrado/i);
});
it('tools/list exposes the 7 read tools', async () => {
  const { json } = await rpc(makeDb(), { jsonrpc: '2.0', id: 1, method: 'tools/list' });
  const names = json.result?.tools.map((t) => t.name) ?? [];
  for (const n of ['search_processos', 'get_processo', 'search_clientes', 'get_cliente', 'list_prazos', 'list_tarefas', 'get_dashboard']) {
    expect(names).toContain(n);
  }
});
```

(escrever os 3 testes com fakeDb completo; rodar → FAIL com `-32601 Method not found`.)

- [ ] **Step 3: Run test to verify it fails**

Run: `npx vitest run tests/mcp_tools.test.ts`
Expected: FAIL — `tools/list` não contém os nomes; `tools/call` retorna `-32601`.

- [ ] **Step 4: Write minimal implementation**

Exemplo (`src/mcp/tools/processos.ts`; demais arquivos seguem o mesmo molde com o SQL da rota correspondente):

```typescript
import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { z } from 'zod';
import type { ToolCtx } from '../server';
import { tool } from '../server';

export function registerProcessos(server: McpServer, ctx: ToolCtx): void {
  tool(server, ctx, 'search_processos', 'Busca processos por CNJ, status ou fase', {
    q: z.string().optional(), status: z.string().optional(), fase: z.string().optional(),
  }, async (args) => {
    let sql = `SELECT p.*, cl.nome AS cliente_nome FROM processos p JOIN clientes cl ON cl.id = p.cliente_id WHERE 1=1`;
    const bind: string[] = [];
    if (args.q) { sql += ` AND (p.numero_cnj LIKE ? OR cl.nome LIKE ?)`; bind.push(`%${args.q}%`, `%${args.q}%`); }
    if (args.status) { sql += ` AND p.status = ?`; bind.push(String(args.status)); }
    if (args.fase) { sql += ` AND p.fase = ?`; bind.push(String(args.fase)); }
    sql += ` ORDER BY p.created_at DESC LIMIT 100`;
    const rows = await ctx.db.prepare(sql).bind(...bind).all();
    return rows.results;
  });

  tool(server, ctx, 'get_processo', 'Ficha completa do processo', {
    id: z.string(),
  }, async (args) => {
    const p = await ctx.db.prepare(`SELECT * FROM processos WHERE id = ?`).bind(String(args.id)).first();
    if (!p) throw new Error('Processo não encontrado.');
    return p;
  });
}
```

`clientes.ts`: `search_clientes` (LIKE por nome), `get_cliente` (+ processos, erro 'Cliente não encontrado.'). `prazos.ts`: `list_prazos` (status/processo_id, mesmos valores da rota). `tarefas.ts`: `list_tarefas`. `dashboard.ts`: `get_dashboard` (mesmo SELECT/agregação da rota dashboard — copiar o corpo do handler).

`server.ts`: importar e chamar os 5 registers dentro de `buildMcpServer`.

- [ ] **Step 5: Run test to verify it passes**

Run: `npx vitest run tests/mcp_tools.test.ts` — Expected: PASS.
Run: `npm test` — Expected: suíte inteira verde.

- [ ] **Step 6: Commit**

```bash
git add src/mcp/ src/routes/ tests/mcp_tools.test.ts
git commit -m "feat: MCP tools de leitura do portal

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 4: Tools de escrita (9)

**Files:**
- Modify: `src/mcp/tools/processos.ts`, `clientes.ts`, `prazos.ts`, `tarefas.ts` (adicionar registers)
- Test: `tests/mcp_tools.test.ts` (anexar parte 2)

**Interfaces:**
- Consumes: `tool()`, schemas exportados (validação via `schema.parse`, mesmos códigos `invalid_*` convertidos em `Error` legível).
- Produces: 9 tools; `tools/list` total = 16.

- [ ] **Step 1: Write the failing tests** (anexar ao mesmo arquivo)

```typescript
it('create_cliente validates email', async () => {
  const { json } = await rpc(makeDb(), { jsonrpc: '2.0', id: 4, method: 'tools/call',
    params: { name: 'create_cliente', arguments: { nome: 'X', email: 'sem-arroba' } } });
  expect(json.result.isError).toBe(true);
});
it('delete_cliente with processos is blocked', async () => {
  // fakeDb: COUNT(*) → { n: 2 }
  const { json } = await rpc(dbComFilhos, { jsonrpc: '2.0', id: 5, method: 'tools/call',
    params: { name: 'delete_cliente', arguments: { id: 'cli_1' } } });
  expect(json.result.isError).toBe(true);
  const text = json.result.content[0].text as string;
  expect(text).toMatch(/processos vinculados/i);
});
it('tools/list totals 16 tools', async () => {
  const { json } = await rpc(makeDb(), { jsonrpc: '2.0', id: 1, method: 'tools/list' });
  expect(json.result?.tools.length).toBe(16);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/mcp_tools.test.ts`
Expected: FAIL — `-32601` nos novos nomes; total ≠ 16.

- [ ] **Step 3: Write minimal implementation**

Padrão por tool (exemplo em `clientes.ts`):

```typescript
import { schema as clienteSchema } from '../routes/clientes';

tool(server, ctx, 'create_cliente', 'Cadastra cliente com ficha completa', clienteSchema.shape, async (args) => {
  const body = clienteSchema.parse(args);
  const id = `cli_${Math.random().toString(36).slice(2, 10)}`;
  await ctx.db.prepare(
    `INSERT INTO clientes (id, nome, tipo, cpf_cnpj, doc_extra, email, telefone, endereco, contato, observacoes, honorario_status, honorario_valor, honorario_vencimento, honorario_forma) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`
  ).bind(id, body.nome, body.tipo, /* ...restante na ordem do INSERT da rota... */).run();
  return { id, ...body };
});
```

(Converter `ZodError` em `Error` legível: envolver o `parse` em try/catch e relançar `new Error('Dados inválidos: ...')` — usar `e.issues[0]?.message`.)

Lista exata das 9: `create_processo`, `update_processo`, `create_cliente`, `update_cliente`, `delete_cliente` (checar filhos → `throw new Error('Este cliente tem processos vinculados.')`), `create_prazo`, `update_prazo_status`, `create_tarefa`, `update_tarefa`.

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run tests/mcp_tools.test.ts` — Expected: PASS.
Run: `npm test` — Expected: suíte inteira verde.

- [ ] **Step 5: Commit**

```bash
git add src/mcp/ tests/mcp_tools.test.ts
git commit -m "feat: MCP tools de escrita do portal

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 5: Deploy staging + fumaça

**Files:** nenhum (só comandos + conferência).

- [ ] **Step 1: Apply migration em staging**

Run: `npx wrangler d1 migrations apply orbis_db_staging --remote --env staging`
Expected: `0006_api_tokens.sql ✅` (conta Orbis já está em `wrangler.jsonc`).

- [ ] **Step 2: Deploy**

Run: `npx wrangler deploy --env staging`
Expected: `Deployed orbis-portal-staging` + URL `https://orbis-portal-staging.orbis-d36.workers.dev`.

- [ ] **Step 3: Criar token real e testar de ponta a ponta**

```bash
B=https://orbis-portal-staging.orbis-d36.workers.dev
curl -s -c /tmp/mcpjar -X POST $B/api/login -H 'content-type: application/json' \
  -d '{"email":"escritorio@demo.com.br","pass":"demo123"}' -o /dev/null
TOKEN=$(curl -s -b /tmp/mcpjar -X POST $B/api/tokens -H 'content-type: application/json' \
  -d '{"nome":"smoke-mcp"}' | python3 -c "import sys,json; print(json.load(sys.stdin)['token'])")
curl -s -X POST $B/mcp -H 'content-type: application/json' \
  -H 'accept: application/json, text/event-stream' -H "authorization: Bearer $TOKEN" \
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/list"}' | grep -o '"name":"[a-z_]*"' | sort
```

Expected: os 16 nomes, incluindo `search_processos` e `delete_cliente`.

```bash
curl -s -X POST $B/mcp -H 'content-type: application/json' \
  -H 'accept: application/json, text/event-stream' -H "authorization: Bearer $TOKEN" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"get_dashboard","arguments":{}}}'
```

Expected: JSON do resumo (não erro).

- [ ] **Step 4: Revogar o token de fumaça**

```bash
ID=$(curl -s -b /tmp/mcpjar $B/api/tokens | python3 -c "import sys,json; print([t['id'] for t in json.load(sys.stdin)['data'] if t.get('nome')=='smoke-mcp'][0])")
curl -s -X DELETE -b /tmp/mcpjar $B/api/tokens/$ID; rm -f /tmp/mcpjar
```

Expected: `{"ok":true}`.

---

## Self-Review

1. **Spec coverage:** transporte (Task 2) · tokens + CRUD de gerência (Task 1) · 16 tools listadas nominalmente (Tasks 3–4: 7 + 9) · mesmas validações via schemas exportados · sem `delete_processo` · audit por call via helper `tool()` · erros PT-BR legíveis + `isError` · JSON-RPC malformado/405 cobertos pelo SDK e pelo handler · testes TDD + Inspector (opcional manual; fumaça via curl na Task 5) · deploy staging + migration (Task 5) · OAuth/SSE/DO fora do escopo — nenhum task os toca. ✅
2. **Placeholder scan:** sem TBD/TODO; códigos de teste e implementação estão inline; "mesmo molde" da Task 3 refere-se a código-modelo já escrito na própria task. ✅
3. **Type consistency:** `ToolCtx = { db: D1Database; userId: string }` usado igual nas Tasks 2–4; `tool(server, ctx, name, description, shape, run)` mesma assinatura; `verifyBearer(db, header)`; nomes das 16 tools idênticos entre spec, testes e implementação. ✅
